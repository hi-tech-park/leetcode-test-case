
import { minAddToMakeValid } from '../main/[957]-minimum-add-to-make-parentheses-valid'
describe('Minimum Add to Make Parentheses Valid', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
