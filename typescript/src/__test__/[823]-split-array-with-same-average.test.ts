
import { splitArraySameAverage } from '../main/[823]-split-array-with-same-average'
describe('Split Array With Same Average', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
