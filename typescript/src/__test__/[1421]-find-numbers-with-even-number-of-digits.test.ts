
import { findNumbers } from '../main/[1421]-find-numbers-with-even-number-of-digits'
describe('Find Numbers with Even Number of Digits', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
