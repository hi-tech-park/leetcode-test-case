
import { longestDecomposition } from '../main/[1251]-longest-chunked-palindrome-decomposition'
describe('Longest Chunked Palindrome Decomposition', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
