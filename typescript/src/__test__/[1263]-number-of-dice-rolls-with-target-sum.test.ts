
import { numRollsToTarget } from '../main/[1263]-number-of-dice-rolls-with-target-sum'
describe('Number of Dice Rolls With Target Sum', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
