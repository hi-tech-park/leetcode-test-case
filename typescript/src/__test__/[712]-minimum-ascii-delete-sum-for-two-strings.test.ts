
import { minimumDeleteSum } from '../main/[712]-minimum-ascii-delete-sum-for-two-strings'
describe('Minimum ASCII Delete Sum for Two Strings', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
