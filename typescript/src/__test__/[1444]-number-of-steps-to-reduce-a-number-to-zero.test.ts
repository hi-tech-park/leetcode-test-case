
import { numberOfSteps } from '../main/[1444]-number-of-steps-to-reduce-a-number-to-zero'
describe('Number of Steps to Reduce a Number to Zero', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
