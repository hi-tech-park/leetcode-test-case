
import { maxAbsValExpr } from '../main/[1230]-maximum-of-absolute-value-expression'
describe('Maximum of Absolute Value Expression', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
