
import { deckRevealedIncreasing } from '../main/[987]-reveal-cards-in-increasing-order'
describe('Reveal Cards In Increasing Order', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
