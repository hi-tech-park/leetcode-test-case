
import { dominantIndex } from '../main/[748]-largest-number-at-least-twice-of-others'
describe('Largest Number At Least Twice of Others', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
