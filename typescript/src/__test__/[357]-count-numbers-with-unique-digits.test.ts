
import { countNumbersWithUniqueDigits } from '../main/[357]-count-numbers-with-unique-digits'
describe('Count Numbers with Unique Digits', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
