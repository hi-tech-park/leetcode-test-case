
import { criticalConnections } from '../main/[1300]-critical-connections-in-a-network'
describe('Critical Connections in a Network', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
