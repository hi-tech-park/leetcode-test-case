
import { splitIntoFibonacci } from '../main/[872]-split-array-into-fibonacci-sequence'
describe('Split Array into Fibonacci Sequence', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
