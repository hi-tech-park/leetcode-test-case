
import { circularPermutation } from '../main/[1359]-circular-permutation-in-binary-representation'
describe('Circular Permutation in Binary Representation', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
