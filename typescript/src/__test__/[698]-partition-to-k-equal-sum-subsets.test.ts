
import { canPartitionKSubsets } from '../main/[698]-partition-to-k-equal-sum-subsets'
describe('Partition to K Equal Sum Subsets', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
