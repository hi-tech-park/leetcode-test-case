
import { maxSumAfterPartitioning } from '../main/[1121]-partition-array-for-maximum-sum'
describe('Partition Array for Maximum Sum', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
