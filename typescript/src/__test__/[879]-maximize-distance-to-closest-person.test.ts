
import { maxDistToClosest } from '../main/[879]-maximize-distance-to-closest-person'
describe('Maximize Distance to Closest Person', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
