
import { hasAlternatingBits } from '../main/[693]-binary-number-with-alternating-bits'
describe('Binary Number with Alternating Bits', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
