
import { numSpecialEquivGroups } from '../main/[929]-groups-of-special-equivalent-strings'
describe('Groups of Special-Equivalent Strings', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
