
import { shortestSubarray } from '../main/[892]-shortest-subarray-with-sum-at-least-k'
describe('Shortest Subarray with Sum at Least K', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
