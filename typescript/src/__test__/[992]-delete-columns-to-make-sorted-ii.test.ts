
import { minDeletionSize } from '../main/[992]-delete-columns-to-make-sorted-ii'
describe('Delete Columns to Make Sorted II', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
