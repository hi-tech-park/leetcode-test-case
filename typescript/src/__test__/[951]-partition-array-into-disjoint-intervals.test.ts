
import { partitionDisjoint } from '../main/[951]-partition-array-into-disjoint-intervals'
describe('Partition Array into Disjoint Intervals', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
