
import { kthSmallest } from '../main/[378]-kth-smallest-element-in-a-sorted-matrix'
describe('Kth Smallest Element in a Sorted Matrix', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
