
import { maxDepthAfterSplit } from '../main/[1208]-maximum-nesting-depth-of-two-valid-parentheses-strings'
describe('Maximum Nesting Depth of Two Valid Parentheses Strings', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
