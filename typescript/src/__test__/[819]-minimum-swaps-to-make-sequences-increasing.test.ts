
import { minSwap } from '../main/[819]-minimum-swaps-to-make-sequences-increasing'
describe('Minimum Swaps To Make Sequences Increasing', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
