
import { minimumMoves } from '../main/[1322]-minimum-moves-to-reach-target-with-rotations'
describe('Minimum Moves to Reach Target with Rotations', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
