
import { largestTimeFromDigits } from '../main/[986]-largest-time-for-given-digits'
describe('Largest Time for Given Digits', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
