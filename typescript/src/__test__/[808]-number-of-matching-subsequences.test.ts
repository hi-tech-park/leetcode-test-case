
import { numMatchingSubseq } from '../main/[808]-number-of-matching-subsequences'
describe('Number of Matching Subsequences', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
